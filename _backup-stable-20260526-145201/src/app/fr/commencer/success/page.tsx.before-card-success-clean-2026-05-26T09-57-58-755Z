import { Suspense } from "react";
import PaymentSuccessPage from "@/components/PaymentSuccessPage";

export default function FrenchPaymentSuccessPage() {
  return (
    <Suspense fallback={null}>
      <PaymentSuccessPage lang="fr" />
    </Suspense>
  );
}