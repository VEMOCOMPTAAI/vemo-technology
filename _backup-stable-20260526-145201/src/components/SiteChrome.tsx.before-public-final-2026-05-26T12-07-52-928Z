
"use client";

import Link from "next/link";

type Lang = "fr" | "en";

const text = {
  fr: {
    home: "Accueil",
    setup: "Business Setup",
    banking: "Banking",
    services: "Services",
    resources: "Resources",
    pricing: "Tarifs",
    contact: "Contact",
    calculator: "Cost Calculator",
    tagline: "US LLC pour non-résidents",
    footer: "Accompagnement professionnel pour créer, structurer et suivre votre LLC US à distance.",
  },
  en: {
    home: "Home",
    setup: "Business Setup",
    banking: "Banking",
    services: "Services",
    resources: "Resources",
    pricing: "Pricing",
    contact: "Contact",
    calculator: "Cost Calculator",
    tagline: "US LLC for non-residents",
    footer: "Professional support to create, structure and manage your US LLC remotely.",
  },
};

const routes = {
  fr: {
    home: "/fr",
    setup: "/fr/commencer",
    banking: "/fr/banking",
    services: "/fr/services",
    resources: "/fr/resources",
    pricing: "/fr/tarifs",
    contact: "/fr/contact",
    lang: "/en",
  },
  en: {
    home: "/en",
    setup: "/en/commencer",
    banking: "/en/banking",
    services: "/en/services",
    resources: "/en/resources",
    pricing: "/en/pricing",
    contact: "/en/contact",
    lang: "/fr",
  },
};

export function SiteHeader({ lang = "en" }: { lang?: Lang }) {
  const isFr = lang === "fr";
  const t = text[lang];
  const r = routes[lang];

  return (
    <header className="sticky top-0 z-50 border-b border-[#eee7df] bg-white/95 backdrop-blur-xl">
      <div className="mx-auto flex h-[64px] max-w-7xl items-center justify-between px-6">
        <Link href={r.home} className="flex items-center gap-3">
          <div className="leading-none">
            <div className="text-[1.65rem] font-black tracking-[-0.04em]">
              <span className="text-[#123A63]">VEMO</span>
              <span className="text-[#F15A24]">TECH</span>
            </div>
            <div className="mt-0.5 text-[10px] font-black uppercase tracking-[0.16em] text-[#2b2f36]/70">
              {t.tagline}
            </div>
          </div>
        </Link>

        <nav className="hidden items-center gap-7 text-sm font-bold text-[#2b2f36] lg:flex">
          <Link href={r.home} className="hover:text-[#F15A24]">{t.home}</Link>
          <Link href={r.setup} className="hover:text-[#F15A24]">{t.setup}</Link>
          <Link href={r.banking} className="hover:text-[#F15A24]">{t.banking}</Link>
          <Link href={r.services} className="hover:text-[#F15A24]">{t.services}</Link>
          <Link href={r.resources} className="hover:text-[#F15A24]">{t.resources}</Link>
          <Link href={r.pricing} className="hover:text-[#F15A24]">{t.pricing}</Link>
          <Link href={r.contact} className="hover:text-[#F15A24]">{t.contact}</Link>
        </nav>

        <div className="flex items-center gap-4">
          <Link href={r.lang} className="hidden text-sm font-bold text-[#2b2f36] hover:text-[#F15A24] md:inline-flex">
            {isFr ? "EN" : "FR"}
          </Link>

          <Link
            href={r.setup}
            className="rounded-[4px] bg-[#F15A24] px-6 py-3 text-sm font-bold text-white shadow-[0_12px_24px_rgba(241,90,36,0.22)] transition hover:bg-[#d84d1f]"
          >
            {t.calculator}
          </Link>
        </div>
      </div>
    </header>
  );
}

export function SiteFooter({ lang = "en" }: { lang?: Lang }) {
  const t = text[lang];
  const r = routes[lang];

  return (
    <footer className="bg-[#123A63] text-white">
      <div className="mx-auto grid max-w-7xl gap-10 px-6 py-12 md:grid-cols-[1.3fr_1fr_1fr_1fr]">
        <div>
          <Link href={r.home} className="inline-flex text-2xl font-black tracking-[-0.04em]">
            <span>VEMO</span><span className="text-[#F15A24]">TECH</span>
          </Link>
          <p className="mt-4 max-w-sm text-sm font-medium leading-7 text-white/75">{t.footer}</p>
        </div>

        <FooterCol title="Navigation" items={[
          [t.home, r.home],
          [t.pricing, r.pricing],
          [t.contact, r.contact],
        ]} />

        <FooterCol title="Services" items={[
          ["LLC Formation", r.setup],
          ["EIN", r.services],
          ["Banking Guidance", r.banking],
        ]} />

        <FooterCol title="Legal" items={[
          ["Terms", r.resources],
          ["Privacy", r.resources],
          ["Refund Policy", r.resources],
        ]} />
      </div>

      <div className="border-t border-white/10 px-6 py-5 text-center text-xs font-semibold text-white/60">
        © 2026 Vemo Technology. All rights reserved.
      </div>
    </footer>
  );
}

function FooterCol({ title, items }: { title: string; items: [string, string][] }) {
  return (
    <div>
      <h3 className="text-sm font-black">{title}</h3>
      <div className="mt-4 space-y-3">
        {items.map(([label, href]) => (
          <Link key={label} href={href} className="block text-sm font-medium text-white/70 hover:text-white">
            {label}
          </Link>
        ))}
      </div>
    </div>
  );
}
